import 'package:flutter/material.dart';

class MidFilterCard extends StatelessWidget {
  const MidFilterCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
